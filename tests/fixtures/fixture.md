# Sample sermon excerpt

Paragraph one for ingest tests. It should normalize and split into multiple chunks when max chunk size is small.

Paragraph two continues the theme with more words so that fixed windows or paragraph packing produces several chunks with consecutive indices.

Paragraph three wraps up this fixture. ScriptureLM step seven uses markdown sources as UTF-8 text.
